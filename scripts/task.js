class Task{//new library that contains the constructor

    constructor(title, descript, color, startdate, status, budget){//using a constructor to list the attributes
        this.title = title;//title of the task
        this.description = descript;//description of the task
        this.color = color;//color of the task
        this.startdate = startdate;//start date
        this.status = status;// status of the task
        this.budget = budget;//budget of the task
        this.name = "Christina";
    }
}